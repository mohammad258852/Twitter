#ifndef CONSTS
#define CONSTS

#define MAX 1024
#define ARRAY_SIZE(a) (sizeof(a)/sizeof(a[0]))

#define IP "127.0.0.2"
#define PORT 12345

char auth[MAX];
char username[MAX];
char password[MAX];

#endif